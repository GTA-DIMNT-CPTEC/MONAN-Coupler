# upstream/ — Arquivos originários do repositório MOM6+SIS2

Estes arquivos **não devem ser editados localmente**.
Alterações devem ser feitas no repositório oficial do MOM6 e sincronizadas aqui.

| Arquivo | Origem |
|---|---|
| `mom_cap.F90` | `MOM6/config_src/drivers/nuopc_cap/` |
| `mom_cap_methods.F90` | `MOM6/config_src/drivers/nuopc_cap/` |
| `mom_cap_time.F90` | `MOM6/config_src/drivers/nuopc_cap/` |
| `mom_ocean_model_nuopc.F90` | `MOM6/config_src/drivers/nuopc_cap/` |
| `mom_surface_forcing_nuopc.F90` | `MOM6/config_src/drivers/nuopc_cap/` |

> **Atenção:** estes arquivos **não são compilados** por este Makefile.
> Eles são fornecidos pela biblioteca pré-compilada `MOM6_LIBDIR`.
> O diretório existe apenas como referência de código-fonte.
